import styled from 'styled-components';

const StyledWrapper = styled.form`
  position: relative;
  z-index: 10;
  width: 608px;
  height: 443px;
  background: #ecd6f033 0% 0% no-repeat padding-box;
  border: 1px solid #707070;
  border-radius: 16px;
  backdrop-filter: blur(7px);
  -webkit-backdrop-filter: blur(8px);
  color: #ffffff;
  padding: 48px;
  h1 {
    font: normal normal 300 36px/44px Helvetica Neue;
    margin: 0 0 32px 0;
  }
`;

export default StyledWrapper;
